
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.4.1
  creationTimestamp: null
  name: redeployments.platform.plural.sh
spec:
  group: platform.plural.sh
  names:
    kind: Redeployment
    listKind: RedeploymentList
    plural: redeployments
    singular: redeployment
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Redeployment is the Schema for the redeployment API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: RedeploymentSpec is a specification of a redeployment
            properties:
              name:
                description: Name of the application which must be redeployed after
                  secrets or config maps changes
                type: string
              namespace:
                description: Namespace of the application
                type: string
              workflow:
                description: 'The Kubernetes workflow type: DaemonSets, Deployments,
                  StatefulSets'
                enum:
                - daemonset
                - deployment
                - statefulset
                - pod
                type: string
            required:
            - name
            - namespace
            - workflow
            type: object
          status:
            description: RedeploymentStatus defines the observed state of Redeployment
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
