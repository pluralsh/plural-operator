apiVersion: v1
kind: Service
metadata:
  name: {{ include "plural-operator.fullname" . }}
  labels:
    {{- include "plural-operator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "plural-operator.selectorLabels" . | nindent 4 }}
